# Assets

Place `demo.gif` here — screen recording of BrightBar in action:
- F1/F2 keypress showing the OSD
- Dragging the OSD slider with the mouse
- Using the menu bar popover

Record with Cmd+Shift+5, convert with: `ffmpeg -i demo.mov -vf "fps=15,scale=800:-1" -loop 0 demo.gif`
